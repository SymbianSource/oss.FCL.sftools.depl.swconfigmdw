Tests:
This tests variations in the Shapes XML file:
- Defining global custom patterns: Green diagonal stripes
- Referencing custom patterns
- Defining custom shapes
- Named shapes: round and hexagon
- Sorting borders in Legend
- Not all borders displayed in legend (onyl A, B, D and E appear).
- Example syntax of inline custom pattern.
- Double normal default component border width
- Test unused invalid colour name ("Spoon")


Differences:

The legend in ini-output.svg does not have the overridden labels nor the notes. The order of legend items also differs.


Telephony server collection is blue in model-output.svg. Unsupported feature <display bg="blue" name="Telephony Server"/> is not possible using ini files.